spring.application.name=Every Dollar

spring.datasource.url=jdbc:mysql://localhost:3306/everydollar_db
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.security.user.name=admin
spring.security.user.password=admin

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


server.servlet.session.timeout=30m
# server.servlet.session.cookie.http-only=true       # Prevent JavaScript access to cookies
# server.servlet.session.cookie.secure=false         # Set to true if using HTTPS
               


# spring.security.enabled=false
